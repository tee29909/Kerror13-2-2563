@page
@model tbkkData.ListModel

@{
    ViewData["Title"] = "List";
    
}
    <script>
                                document.getElementById("name").innerHTML = "@Model.Employees[0].FirstName.ToString()";
                                document.getElementById("lastname").innerHTML = "@Model.Employees[0].LastName.ToString()";
                                document.getElementById("position").innerHTML = "@Model.Employees[0].Position.PositionName.ToString()";
                                document.getElementById("name1").innerHTML = "@Model.Employees[0].FirstName.ToString()";
                                document.getElementById("lastname1").innerHTML = "@Model.Employees[0].LastName.ToString()";
                                document.getElementById("position1").innerHTML = "@Model.Employees[0].Position.PositionName.ToString()";
    </script>
<h2>List</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Manufacture[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Manufacture[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Manufacture[0].Product)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Manufacture[0].Employee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Manufacture[0].Employee.Company)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Manufacture) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.FirstName)   @Html.DisplayFor(modelItem => item.Employee.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.Company.CompanyName)   @Html.DisplayFor(modelItem => item.Employee.Company.Category)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ManufactureID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ManufactureID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ManufactureID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
